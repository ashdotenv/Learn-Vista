export const {
  PORT,
  ORIGIN,
  CLOUDINARY_API_SECRET,
  CLOUDINARY_CLOUD_NAME,
  CLOUDINARY_API_KEY,
  DB_URI,
  REDIS_URI,
  ACTIVATION_SECRET,
  JWT_SECRET,
  JWT_EXPIRES_IN,
  SMTP_HOST,
  SMTP_PORT,
  SMTP_USER,
  SMTP_PASS,
  FROM_EMAIL,
  ACCESS_TOKEN,
  REFRESH_TOKEN,
  REFRESH_TOKEN_EXPIRES,
  ACCESS_TOKEN_EXPIRES,
  NODE_ENV,
} = process.env;
